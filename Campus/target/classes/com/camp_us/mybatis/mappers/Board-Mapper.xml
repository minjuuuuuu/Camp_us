<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.camp_us.mapper.BoardMapper">

    <!-- 게시글 목록 조회 -->
    <select id="selectBoardList" resultType="com.camp_us.dto.BoardVO">
        SELECT 
            BNO, 
            CATEGORY, 
            TITLE, 
            WRITER, 
            REGDATE
        FROM BOARD
        ORDER BY REGDATE DESC
    </select>

</mapper>
